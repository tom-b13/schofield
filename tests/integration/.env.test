# Integration test configuration for local runs
# Clarke: Provide valid TEST_BASE_URL and TEST_DATABASE_URL so environment hook passes.
# The environment hook will auto-start a local Uvicorn server if nothing is
# listening at TEST_BASE_URL. The SQLite DSN below satisfies connectivity checks
# without requiring a local Postgres instance.
TEST_BASE_URL=http://127.0.0.1:8000
TEST_DATABASE_URL=sqlite:///./tmp/questionnaire_integration.db

